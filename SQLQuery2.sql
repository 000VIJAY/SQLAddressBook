create database AddressBook
use AddressBook
create table Addressbook
(
	FirstName	varchar(50),
	LastName	varchar (50),
	Address		varchar(150),
	City		varchar(25),
	State		varchar(25),
	zip			BigInt,
	PhoneNumber BigInt,
	Email		varchar(50)
	)
	SELECT * FROM Addressbook;